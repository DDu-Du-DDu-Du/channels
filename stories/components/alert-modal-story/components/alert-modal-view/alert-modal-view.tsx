import { AlertModal } from "@/components";
import { AlertModalProps } from "@/components/alert-modal/alert-modal";

function AlertModalView(props: AlertModalProps) {
  return <AlertModal {...props} />;
}

export default AlertModalView;
